export enum AppPackges {
    LAUNCHER = 'com.amazon.tv.launcher',
    SETTINGS = 'com.amazon.tv.settings',
}

export enum Intends {
    LAUNCH = 'android.intent.category.LAUNCHER',
    HOME = 'android.intent.category.HOME'
}